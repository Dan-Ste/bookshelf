import fbUploadImage from 'bookshelf/utils/fb-upload-image';
import { module, test } from 'qunit';

module('Unit | Utility | fb upload image');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fbUploadImage();
  assert.ok(result);
});
