import bookStates from 'bookshelf/utils/book-states';
import { module, test } from 'qunit';

module('Unit | Utility | book states');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = bookStates();
  assert.ok(result);
});
