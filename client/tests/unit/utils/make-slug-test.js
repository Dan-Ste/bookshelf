import makeSlug from 'bookshelf/utils/make-slug';
import { module, test } from 'qunit';

module('Unit | Utility | make slug');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = makeSlug();
  assert.ok(result);
});
