{{ui-checkbox
    label=title
    type='radio'
    class="radio"
    local-class=(concat 'state' stateId)
    name='book-states'
    checked=(eq (concat bookState) stateId)
    onChange=(action onChange stateId)
}}